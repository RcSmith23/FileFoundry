<div class="navbar navbar-custom navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
	<!--<#%= link_to "FileFoundry", root_path, :class => "navbar" %>-->
    </div>
    <div id="navbar navbar-left" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <%= nav_link "Home", root_path  %>
        <%= nav_link "Collection", drawings_path %>
      </ul>
      <ul class="nav navbar-nav navbar-right" style="margin-left: 1em;">
        <% if user_signed_in? %>
         <span class="user-email">  <%=link_to current_user.email,current_user %>
	<%= link_to "Logout", destroy_user_session_path, :method => :delete %> </span>
        <% else %>
          <!--<%= nav_button "Sign Up", new_user_registration_path %>-->
          <%= nav_button "Log in", user_session_path %>
        <% end %>
      </ul>
    </div>
  </div>
  <%= render 'layouts/flash' %>
</div>
